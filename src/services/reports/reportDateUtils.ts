export type ReportDateRange = { start: string; end: string; label: string }
const localDate = (date: Date) => { const year = date.getFullYear(); const month = String(date.getMonth() + 1).padStart(2, '0'); const day = String(date.getDate()).padStart(2, '0'); return `${year}-${month}-${day}` }
export function getReportRange(filter: string, customStart = '', customEnd = ''): ReportDateRange { const today = new Date(); today.setHours(0, 0, 0, 0); const end = localDate(today); const start = new Date(today); if (filter === 'Yesterday') { start.setDate(start.getDate() - 1); return { start: localDate(start), end: localDate(start), label: 'Yesterday' } } if (filter === 'This Week') start.setDate(start.getDate() - 6); if (filter === 'This Month') start.setDate(1); if (filter === 'Last 30 Days') start.setDate(start.getDate() - 29); if (filter === 'Custom Date Range' && customStart && customEnd) return { start: customStart, end: customEnd, label: 'Custom Date Range' }; return { start: localDate(start), end, label: filter } }
export const inReportRange = (date: string, range: ReportDateRange) => date >= range.start && date <= range.end
export const reportFilters = ['Today', 'Yesterday', 'This Week', 'This Month', 'Last 30 Days', 'Custom Date Range']
