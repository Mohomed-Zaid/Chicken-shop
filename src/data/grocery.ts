export interface GroceryProduct{id:string;name:string;category:string;barcode:string;costPrice:number;sellingPrice:number;stockQuantity:number;lowStockLevel:number;unit:string;active:boolean;createdAt:string;updatedAt:string}
const key='grocery-products';const now=()=>new Date().toISOString();export const categories=['Grocery','Beverages','Biscuits','Dairy','Rice','Spices','Snacks','Household','Cleaning','Other'];export const demo:GroceryProduct[]=[['Sugar 1kg','Grocery','4790012345678',220,250,50,10],['Milk Powder','Dairy','4790012345679',1000,1200,20,5],['Biscuits','Biscuits','4790012345680',100,150,100,20],['Rice 5kg','Rice','4790012345681',1200,1400,15,5]].map(([name,category,barcode,costPrice,sellingPrice,stockQuantity,lowStockLevel],i)=>({id:`grocery-${i+1}`,name:name as string,category:category as string,barcode:barcode as string,costPrice:costPrice as number,sellingPrice:sellingPrice as number,stockQuantity:stockQuantity as number,lowStockLevel:lowStockLevel as number,unit:'Piece',active:true,createdAt:now(),updatedAt:now()}));export const groceryStore={load:():GroceryProduct[]=>{try{const raw=localStorage.getItem(key);return raw?JSON.parse(raw):demo}catch{return demo}},save:(items:GroceryProduct[])=>localStorage.setItem(key,JSON.stringify(items))};